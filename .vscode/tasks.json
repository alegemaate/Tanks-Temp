{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "detail": "Build and run",
      "command": "cmake --build . && cd build && ./Tanks",
      "windows": {
        "command": "cmake --build . && cd build && Tanks.exe"
      },
      "group": "build",
      "problemMatcher": "$gcc"
    }
  ]
}
